<div class="container">
  <%= flash[:notice] %>

  <div class="col-md-12">
  <h1><%= @post.title %></h1>
  <p><%= @post.content %></p>
  <div class="col-md-12">

    <h3>Comments</h3>
    <ul>
      <% if @post.comments.any? %>

        <% @post.comments.each do |comment| %>
            <li><%= comment.comment_text %></li>
            <%= link_to "Edit", edit_post_comment_path(comment.post_id, comment.id) %> |
            <%= link_to "Delete", post_comment_path(comment.post_id, comment.id),
                                  :data => {:confirm => "Delete this comment?",
                                            :method => "delete"} %>
    <br>
        <% end %>
      <% end %>
    </ul>
    <hr>

  <%= link_to "Add a Comment", new_post_comment_path(@post), class: "btn btn-sm btn-warning" %>

  <hr>

  | <%= link_to "Home", posts_path, :class => "btn btn-sm btn-primary" %>
  | <%= link_to "Edit", edit_post_path,
                              :class => "btn btn-sm btn-primary" %>
  | <%= link_to "Delete", post_path(@post), class: "btn btn-sm btn-primary",
                              :data => { :confirm => "Delete this blog post?",
                                      :method => "delete"} %>
  |
</div>
